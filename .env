# --- CORE DJANGO CONFIGURATION ---

# CRITICAL: Replace this with a long, unique, random string.
# DO NOT USE THE EXAMPLE KEY. Django won't run without a valid key.
SECRET_KEY='your_very_long_random_django_secret_key_here_e_g_use_a_generator'

# Set to False when deploying to a live server
DEBUG=True

# Defines which hostnames the server is allowed to serve.
ALLOWED_HOSTS='127.0.0.1,localhost'


# --- EMAIL SERVER CONFIGURATION (MANDATORY) ---
# Used for: Two-Factor Authentication (2FA), Shift Notifications, Mass Messaging.

# Use standard SMTP server settings (e.g., for Gmail, Outlook, or a custom host)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True

# --- TRAINING PLATFORM (OPTIONAL) ---
TRAINING_PROVIDER=alternative_lms  # (once implemented)

# This is the email address that will be displayed as the sender
EMAIL_HOST_USER=your.dept.email@gmail.com

# CRITICAL: This is the password for the email account.
# If using Gmail, you MUST generate an "App Password" through your Google Account security settings.
EMAIL_HOST_PASSWORD=YOUR_APP_PASSWORD_OR_API_KEY_HERE
