# CORE DJANGO AND WEB SERVER
Django>=4.2,<5.0
gunicorn==20.1.0  # For production deployment (optional for local dev)

# DATABASE
# Since we are using sqlite3 (default), we don't need a specific package here.
# If using PostgreSQL, we would add: psycopg2-binary

# CONFIGURATION & SECURITY
python-decouple==3.8  # For reading the .env file
django-environ==0.11.2 # Alternative/robust .env handler (we'll use this)
django-otp==1.2.1     # Base package for One-Time Password framework
django-two-factor-auth==1.15.0 # Integrates two-factor authentication (required for staff)

# SCHEDULING & TIME
python-dateutil==2.8.2 # Robust date/time parsing
icalendar==5.0.12      # For generating downloadable .ics calendar files

# EMAIL
django-sendgrid-v5==3.0.0 # If using SendGrid (we configured for general SMTP/Gmail, so keep it for compatibility)
