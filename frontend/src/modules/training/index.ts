/**
 * Training Module
 *
 * This module handles training management for the department,
 * including training records, certifications, course library,
 * training programs/pipelines, skills testing, and admin tools.
 *
 * To enable/disable this module, simply include or exclude the
 * getTrainingRoutes function call in your main App.tsx routing.
 */

// Export routes
export { getTrainingRoutes } from './routes';
