/**
 * Events Module
 *
 * This module handles event management for the department,
 * including event listings, detail views, QR check-in,
 * admin hub, analytics, and monitoring.
 *
 * To enable/disable this module, simply include or exclude the
 * getEventsRoutes function call in your main App.tsx routing.
 */

// Export routes
export { getEventsRoutes, getEventsPublicRoutes } from './routes';
