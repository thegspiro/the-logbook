/**
 * Onboarding Module
 *
 * This module handles the first-time setup wizard for The Logbook.
 * It collects department information, configuration preferences,
 * and creates the initial admin user.
 *
 * To enable/disable this module, simply include or exclude the
 * OnboardingRoutes component in your main App.tsx routing.
 */

// Export routes
export { OnboardingRoutes } from './routes';

// Export pages (if needed for direct use)
export * from './pages';

// Export components
export * from './components';

// Export hooks
export * from './hooks';

// Export types
export * from './types';

// Export utilities
export * from './utils';
