/**
 * Documents Module
 *
 * Handles document management including folders, uploads, and versioning.
 *
 * To enable/disable this module, simply include or exclude the
 * getDocumentsRoutes function call in your main App.tsx routing.
 */

export { getDocumentsRoutes } from './routes';
