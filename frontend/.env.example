# API Configuration
# VITE_API_URL: The base URL used by the frontend to make API requests
# VITE_BACKEND_URL: The backend server URL used by Vite dev server proxy (development only)

# Option 1: Standard Development (Vite dev server with proxy)
# Frontend makes requests to /api/v1, Vite proxies to backend
VITE_API_URL=/api/v1
VITE_BACKEND_URL=http://localhost:3001

# Option 2: Production/Docker with Nginx Proxy (RECOMMENDED)
# Use relative URL - nginx will proxy /api requests to backend
# VITE_API_URL=/api/v1
# (VITE_BACKEND_URL is not needed in production - nginx handles proxying)

# Option 3: Direct Backend Access (no proxy, not recommended)
# VITE_API_URL=http://localhost:3001/api/v1
# Or use your server IP:
# VITE_API_URL=http://YOUR-SERVER-IP:7881/api/v1

# WebSocket Configuration (if using real-time features)
VITE_WS_URL=ws://localhost:3001
# For Unraid: ws://localhost:7881 or ws://YOUR-UNRAID-IP:7881

# Environment
VITE_ENV=development

# Security - Session Encryption Key (CHANGE THIS IN PRODUCTION!)
VITE_SESSION_KEY=change-this-to-a-random-32-character-string-in-production

# Feature Flags
VITE_ENABLE_PWA=true
VITE_ENABLE_ANALYTICS=false
