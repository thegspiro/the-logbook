# Advisories deliberately not fixed. Every entry must have a matching section
# in docs/KNOWN_LIMITATIONS.md explaining why, and must be removed the moment
# the blocker clears. Do not add an entry just to make the scan pass.

# cryptography 49.0.0 — fix is 50.0.0, which cannot be installed: fastapi-mail
# 1.6.5 (the latest release) pins cryptography >=49.0.0,<50.0.0, so pip's
# resolver rejects the pair outright. Blocked until fastapi-mail widens it.
# See docs/KNOWN_LIMITATIONS.md § cryptography is held at 49.x by fastapi-mail.
CVE-2026-69247
